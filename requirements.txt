# CraftifyX Miner 6.0 — Python 依赖
# 安装: pip install -r requirements.txt

wrapt>=2.0.0

# Web 框架
flask==3.1.*
gunicorn==23.*

# 异步 HTTP
aiohttp==3.11.*
httpx==0.28.*

# 数据库
asyncpg==0.30.*
psycopg2-binary==2.9.*
sqlalchemy==2.0.*

# LLM (统一 OpenAI 兼容接口)
openai==1.66.*

# Apify（须与 apify-client 1.8 匹配的 apify-shared 1.x，避免 pip 解析到 2.x 导致 ImportError）
apify-client==1.8.*
apify-shared>=1.1.2,<2

# Dashboard
streamlit==1.41.*
plotly==6.0.*

# 数据处理
pandas==2.2.*
openpyxl==3.1.*

# ML / 评估（pipeline/sps_model、pipeline/model_monitor：Ridge、XGBoost、Spearman 等）
numpy>=1.26,<3
scipy>=1.11,<2
scikit-learn>=1.4,<2
xgboost>=2.0,<3

# 配置
pyyaml==6.0.*
python-dotenv==1.0.*

# 定时任务
apscheduler==3.10.*

# 认证与安全
bcrypt==4.2.*
PyJWT==2.9.*
flask-limiter==3.8.*
